export enum SOCKET_EVENTS {
  CONFIG = 'config',
  ERROR = 'error',
  STATUS = 'status',
  ADS = 'ads',
  COMPLETE = 'complete',
  START = 'start',
  STOP = 'stop',
}

export enum PARSE_STATUS {
  PARSING = 'parsing',
  IDLE = 'idle',
}
